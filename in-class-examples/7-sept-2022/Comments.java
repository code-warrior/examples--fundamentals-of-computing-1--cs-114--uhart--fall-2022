public class Comments {
    // Here is a single-line comment that really
    // shouldn’t span more than one line, but it does!

    /*
     * Here is a true, multi-line comment that VS Code
     * helps with, because, when I hit the return key,
     * VS Code precedes the line with an asterisk.
     */

     /**
      * Here is a javadoc comment, which, in essence is
      * a multi-line comment with some extras.
      * @param args
      */
    public static void main(String args[]) {
        System.out.println("Working with comments, both single- and multi-line.");
    }
}
